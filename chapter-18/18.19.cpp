// std::swap(v1.mem1, v2,mem1);
// using std::swap;
// no matter what type of data member mem1 is,
// this would do work of coping, assignment and possibly memory allocating,
// which may seems to be unnecessary.
