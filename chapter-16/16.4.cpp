#include <vector>
#include <list>

using std::vector;
using std::list;

template <typename T1, typename T2>
new_find(, T2 var)
{
  while(it1 != it2)
    {
      if(*it1 == var)
	return it1;
      else
	++it1;
    }
}
