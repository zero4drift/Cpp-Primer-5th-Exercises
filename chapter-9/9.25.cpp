// c.erase(elem1, elem2)
// if elem1 and elem2 ponits to same element, the iterator range is empty, so erase nothing, return ?
// if elem2 is a end off iterator, remove the one pointed by elem1 and till the last one, return elem2
// if elem1 and elem2 both end off iterator, remove nothing, return ?
