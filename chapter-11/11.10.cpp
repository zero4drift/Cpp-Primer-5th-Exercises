// map<vector<int>::iterator, int> is ok, for that vector<int>::iterator supports relationship operators like <, <=;
// while map<list<int>::iterator, int> is impossible, for that list<int>::iterator supports relationship operators of == and !=.
