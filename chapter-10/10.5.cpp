// '==' operator for two C type strings compares the pointers point to them;
// if the pointers compared points to different objects, that is an undefined behaviour, the result is undefined either;
// else there would be a legal result.
