// For self assignment of Token type object, since the data member tok is exactly the same;
// there is no call of constructor of string type, for any member of union the correspond assignment operation would do the job.
