// The return type and parameters type are different, and function implemention differs;
// The logic meaning are identical, for example, the overload operator should return a reference if the related built-in operator returns a reference.
