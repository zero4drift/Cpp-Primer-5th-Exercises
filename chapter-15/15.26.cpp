// See Quote.h and Bulk_quote.h.

#include "Quote.hpp"
#include "Bulk_quote.hpp"

int main()
{
  Quote q;
  // Bulk_quote b;
  // Quote q2 = q;
  // Quote q3 = std::move(q);
  // q = q2;
  // q = std::move(q3);
  // Bulk_quote b2 = b;
  // Bulk_quote b3 = std::move(b);
  // b = b2;
  // b = std::move(b2);
}
